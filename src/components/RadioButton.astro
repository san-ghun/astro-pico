---
interface Props {
  purpose: string;
  label: string;
  id: string;
  name: string;
  value: string;
  checked: string;
}

const { purpose, label, id, name, value, checked } = Astro.props;
---

<label for={purpose}>
  {
    checked === "true" ? (
      <input type="radio" id={id} name={name} value={value} checked />
    ) : (
      <input type="radio" id={id} name={name} value={value} />
    )
  }
  {label}
</label>
